node_modules
.next
.git

# Nothing that is not needed to build. `COPY . .` would otherwise carry it into the build
# stage's cache and, for anything under public/, into the image.
shipkit.yaml
README.md

# The browser suite and its config. They are the e2e stage's input, not the image's: the kit
# mounts the repository into the browsers image separately.
e2e
playwright.config.ts
