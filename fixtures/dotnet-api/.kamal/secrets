# Local simulation only. Real deployments read these from the CI secret store; per-client
# GitHub accounts mean per-client secret stores, and where each one lives has to be written
# down — it is the part that rots first (ci-cd-plan.md §8).
KAMAL_REGISTRY_PASSWORD=unused-by-a-local-registry
POSTGRES_PASSWORD=postgres
